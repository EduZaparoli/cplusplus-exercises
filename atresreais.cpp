#include <iostream>

using namespace std;

int main() {

    int N, X, Y, Z, SOMA;
    cin >> N;
    while (N--){
        cin >> X >> Y >> Z;
    }
    SOMA = X + Y + Z;
    cout << SOMA << endl;

return 0;
}
