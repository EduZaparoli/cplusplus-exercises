#include <iostream>

using namespace std;

int main(){

    int dados[0], media;
    int i;

    for(i=0; dados>=0; i++){

        cin >> dados[i];

        if(dados[i] < 0){

        break;

        }

    }
        media = (dados[i] + dados[i]) / i;
        cout << media << endl;

return 0;
}
